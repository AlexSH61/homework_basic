fmt.Println(input sideLenght)
if проверка количество переменных
if 2 value (отрицательные, равные нулю)
	fmt.Println(input shape)
	if Err return error (другие фигуры)
	передать в пакет для вычесления площади фигуры
else if 1 value (проверка ошибок, ввод нуля, ввод отрицательные)
if err проверка ошибок
закрыть сканирование exite
*****
пакет для фигур (структуры методы)
вывести в пакет printerShape
	вызвать в main